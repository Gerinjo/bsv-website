{
  "name": "bsv-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/check-built-paths.mjs",
    "test": "node --test tests/*.test.mjs",
    "check:paths": "node scripts/check-built-paths.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "supabase": "^2.111.0"
  }
}
